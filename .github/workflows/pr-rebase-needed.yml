name: "PR Needs Rebase"

on:
  push:
  pull_request_target:
    types: [synchronize]

permissions:
  # Label pull requests.
  pull-requests: write

jobs:
  label-rebase-needed:
    uses: mdn/workflows/.github/workflows/pr-rebase-needed.yml@5c0fe55eace1852dc2a10875c3a399750f5e72be # main
    with:
      target-repo: "mdn/content"
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
